const fs = require('fs');
const input = fs.readFileSync("./코딩테스트/input.txt", "utf8");

console.log(input);

const TESTCASE = 5; // 상수는 보통 대물자로 표기

// 한 줄에 여러 변수 선언함
let name = 'seongjin',
    age = 27,
    msg = 'hello';

    console.log(age);