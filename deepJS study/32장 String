Sting 생성자 함수를 new 연산자와 함께 쓰면 인수로 전달받은 문자열을 할당한 String 래퍼 객체를 생성한다.
인덱스를 나타내는 문자열을 프로퍼티의 키로, 각 문자를 프로퍼티 값으로 갖는 유사 배열 객체이면서 이터러블이다.
문자열이 아닌 값을 전달하면 문자열로 강제변환함. new연산자를 쓰지 않을경우 객체를 생성하지 않으므로 string 인스턴스가 아닌 문자열을 반환한다.
string 래퍼 객체는 읽기 전용 객체다. 때문에 String 객체의 메서드는 언제나 새로운 문자열을 생성하여 반환하는것.

charAt: 대상 문자열에서 인수로 전달받은 인덱스에 위치한 문자를 검색해서 반환한다.
substring과 slice는 동일하게 동작하지만 slice의 경우 음수의 인수를 전달 할 수 있다. substring은 음수 인수 전달시 0으로 처리

이터러블: 자료를 반복할 수 잇는 객체
유사배열객체: 마치 배열처럼 인덱스로 프로퍼티 값에 접근할 수 있고, length 프로퍼티를 갖는 객체를 말한다.

