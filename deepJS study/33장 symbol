 심벌 값은 외부로 노출되지 않아 확인할 수 없으며 다른 값과 절대 중복되지 않는 유일무이한 값이다.
 심벌 값은 암묵적으로 문자열이나 숫자 타입으로 변환되지 않는다.
 불리언 타입으로는 암묵적 타입변환이 된다.

 심벌.for 메소드를 사용하면 전역 심벌 레지스트리에 등록을 할 수 있다.
 심벌.keyfor를 사용하면 전역 심벌 레지스트리에 저장된 심벌 값을 추출할 수 있다.
 일반 심벌 함수를 호출할 시 전역 심벌 레지스트리에 등록되어 관리되지 않는다.

 변경/중복될 가능성이 있는 경우 심벌값을 사용해 방지할 수 있다.
 객체의 경우 프로퍼티 키를 심벌 값으로 만들 수 있다. 이 경우에 다른 프로퍼티 키와 절대 출동하지 않는다는 장점이 있다. 대괄호를 사용해 프로퍼티값을 지정해준다.
 심벌값으로 프로퍼티를 지정해줄 경우 외부에 노출 하지 않아도 되는 프로퍼티를 생성할 수 있다.