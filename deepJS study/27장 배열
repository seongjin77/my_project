배열을 length 프로퍼티를 갖는다.
배열도 객체다. 따라서 배열도 프로토타입을 갖는다.
일반 객체와 배열의 큰 차이점은 '값의 순서' 와 'length 프로퍼티'이다

일반적 의미의 배열은 하나의 데이터 타입으로 통일되어 있으며 서로 인접해 있다. 즉 메모리주소가 연속적이며 동일한 크기의 메모리 공간이 연속으로 나열된 자료구조. 인덱스를 통해 단 한번의 연산으로 임의의 요소에 접근 할 수 있다. 시간복잡도가 매우 낮음.
정렬되지 않은 배열은 배열의 모든 요소를 처음부터 특정 요소까지 검색해야해서 시간복잡도가 올라간다.

자바스크립트에서의 배열은 각각의 메모리 공간이 동일한 크기가 아니여도 되고, 연속적으로 이어져 있지 않을 수도 있다. 배열의 요소가 연속적으로 이어지지 않은 경우의 배열을 희소 배열이라 한다. 정확하게 말하자면 자바스크립트의 배열은 배열을 흉내낸 객체다.

일반적인 배열과 자바스크립트 배열의 차이
자바스크립트 배열은 인덱스로 배열 요소에 접근하는 경우 일반적인 배열보다 느리지만, 요소를 삽입 or 삭제하는 경우 일반적인 배열보다 빠르다.

배열의 length는 임의적으로 할당할 수 있다. (배열의 길이보다 작은 값을 할당할때만)

희소배열의 길이는 실제 배열 요소의 개수와 일치하지 않을 수 있다.
