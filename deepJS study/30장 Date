UTC: 국제 표준시
Date는 생성자 함수다. Date로 생성한 객체는 날짜와 시간을 나타내는 정수값을 갖는다.
new 연산자와 호출할 경우 현재 날짜와 시간을 가지는 Date 객체를 반환.
new 연산자 없이 호출 할 경우 객체를 반환하지 않고 현재 날짜와 시간 정보를 나타내는 문자열 반환.

new Date(year,month[day,hour,minute,second,millisecond]) 숫자로 인수를 전달할 시 month는 0부터 시작. 문자열로 전달할시 상관없음
getDay: Date 객체의 요일(0~6)을 나타내는 정수를 반환. 0일요일~6토요일
