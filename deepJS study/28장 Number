표준 빌트인 객체인 Number 객체는 생성자 함수 객체다. new 연산자를 사용하면 [[NumberData]] 내부 슬롯에 0을 할당한 Number 래퍼 객체를 생성한다.
Number 생성자 함수의 인수로 숫자가 아닌 값을 전달하면 인수를 숫자로 강제 변환한 후 [[NumberData]] 내부 슬롯에 래퍼 객체를 생성.
인수를 숫자로 변할 수 없으면 NaN을 할당.

Number.MAX_VALUE: 자바스크립트에서 표현할 수 있는 가장 큰 양수 값.
Number.MIN_VALUE: 자바스크립트에서 표현할 수 있는 가장 작은 양수 값.
Numvber.inNaN: 인수로 전달된 값이 NaN인지 확인하여 불리언 값으로 반환.
toFixed(n): 소수점 n번째 숫자까지 반올림해서 나타냄. 인수를 생략하면 기본값 0으로 지정.
toString(n): n진수 문자열을 반환한다. 인수를 생략하면 10진수 문자열로 반환. 

