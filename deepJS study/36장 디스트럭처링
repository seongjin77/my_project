디스트럭처링 할당은(구조 분해 할당) 구조화된 배열과 같은 이터러블 또는 객체를 파괴하여 1개이상 변수에 개별적으로 할당하는 것을 말한다.
할당의 대상은 이터러블이여야 하며 할당 기준을 배열의 인덱스다.
변수에 기본값을 설정할 수 있다. const [a,b,c=3] = [1,2]
기본값보다 할당된 값이 우선한다.

객체의 디스트럭처링 할당은 순서는 상관없고 프로퍼티의 키가 할당 기준이다.
변수를 객체 리터럴로 선언해야 한다.